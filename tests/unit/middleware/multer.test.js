const multer = require("../../../middleware/multer");

//jest.mock("multer");

describe("(UnitTesting): Multer middleware", () => {
  test("should populate response with a file object with destination and filename properties", () => {
    // const req = { file: "../../../public/images/placeholder.png" };
    // const file = {
    //   originalname: "Original_Name.jpg",
    //   mimetype: "image/jpg",
    //   path: "path",
    //   buffer: Buffer.from("one,two,three"),
    // };
    // const callback = jest.fn();
    // multer(req, file, callback);
    //expect(req.file).toBeDefined();
  });
});
